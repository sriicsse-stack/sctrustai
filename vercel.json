{
  "version": 3,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" },
    { "src": "server.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/server.ts" }
  ]
}
